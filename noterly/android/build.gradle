buildscript {
    ext.kotlin_version = '1.8.10' // WAS: 1.7.10

    ext {
        compileSdkVersion = 33
        targetSdkVersion = 33
        minSdkVersion = 19 // INFO: Required for Firebase
        appCompatVersion = "1.4.2" // INFO: Required for background_fetch plugin
    }

    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.2' // WAS: 7.1.2, 7.2.2; 7.3.0 breaks
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        classpath 'com.google.gms:google-services:4.3.15' // INFO: Firebase manual installation
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()

        // INFO: Required for background_fetch plugin
        maven {
            url "${project(':background_fetch').projectDir}/libs"
        }
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
